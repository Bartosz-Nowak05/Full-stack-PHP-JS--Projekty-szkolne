zapytanie 1

SELECT `smok`.`nazwa`, `smok`.`dlugosc`, `smok`.`szerokosc`
FROM `smok`
WHERE `smok`.`pochodzenie` = 'Polska';

zapytanie2

SELECT DISTINCT `smok`.`pochodzenie` FROM `smok` ORDER BY `smok`.`pochodzenie` ASC

zapytanie3

SELECT `parada`.`rok`, AVG(`smok`.`dlugosc`) AS `Średnia długość`
FROM `parada`
	, `smok`
WHERE `parada`.`rok` > '2005';

zapytanie4

ALTER TABLE `parada` ADD `lokalizacja` VARCHAR(100) NOT NULL AFTER `atrakcje`;